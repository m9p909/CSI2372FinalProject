name: CI

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Install C++ Dependencies
        run: sudo apt-get install g++

      - name: "Make Ryan's Assignment Copy"
        run: |
          cd ryan
          echo "Making the game..."
          make game
          echo "Making the tests..."
          make test
          echo "TODO: Run Tests..."
